import styled from 'styled-components';

const StyledCardGroup = styled.div`
	display: grid;
	max-width: 100%;
	grid-template-columns: repeat(auto-fit, minmax(314px, 1fr));
	gap: 1.6rem;
`;

export { StyledCardGroup };
