---
to: <%= directoryPath %>/<%= filename %>.tsx
unless_exists: true
---
import { Styled<%= className %> } from './<%= filename %>.styles';

export interface <%= className %>Props {}

const <%= className %> = ({}: <%= className %>Props) => {
	return (
		<Styled<%= className %>>
		</Styled<%= className %>>
	);
};

export { <%= className %> };
